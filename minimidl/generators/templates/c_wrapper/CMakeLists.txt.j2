# Generated by MinimIDL - CMake Build Configuration
# DO NOT EDIT - This file was automatically generated

cmake_minimum_required(VERSION 3.15)
project({{ namespace.name }}_wrapper VERSION 1.0.0 LANGUAGES CXX C)

# C++ standard
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# C standard
set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS OFF)

# Export compile commands for IDEs
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

# Position independent code
set(CMAKE_POSITION_INDEPENDENT_CODE ON)

# Build type
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE Release CACHE STRING "Build type" FORCE)
endif()

# Library source files
set(WRAPPER_SOURCES
    {{ namespace.name.lower() }}_wrapper.cpp
    {{ namespace.name.lower() }}_wrapper.h
    {{ namespace.name.lower() }}.hpp
)

# Create shared library
add_library({{ namespace.name.lower() }}_wrapper SHARED ${WRAPPER_SOURCES})

# Set export macro
target_compile_definitions({{ namespace.name.lower() }}_wrapper PRIVATE {{ namespace.name | upper }}_EXPORTS)

# Include directories
target_include_directories({{ namespace.name.lower() }}_wrapper
    PUBLIC
        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
        $<INSTALL_INTERFACE:include>
)

# Platform-specific settings
if(WIN32)
    # Use .def file for exports on Windows
    set_target_properties({{ namespace.name.lower() }}_wrapper PROPERTIES
        WINDOWS_EXPORT_ALL_SYMBOLS OFF
    )
    target_sources({{ namespace.name.lower() }}_wrapper PRIVATE {{ namespace.name.lower() }}_exports.def)
else()
    # Use visibility attributes on Unix-like systems
    target_compile_options({{ namespace.name.lower() }}_wrapper PRIVATE -fvisibility=hidden)
endif()

# Compiler warnings
if(MSVC)
    target_compile_options({{ namespace.name.lower() }}_wrapper PRIVATE /W4 /WX)
else()
    target_compile_options({{ namespace.name.lower() }}_wrapper PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()

# Test executable
add_executable({{ namespace.name.lower() }}_test {{ namespace.name.lower() }}_test.c)
target_link_libraries({{ namespace.name.lower() }}_test PRIVATE {{ namespace.name.lower() }}_wrapper)

# Installation rules
include(GNUInstallDirs)

install(TARGETS {{ namespace.name.lower() }}_wrapper
    EXPORT {{ namespace.name }}Targets
    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
)

install(FILES {{ namespace.name.lower() }}_wrapper.h
    DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
)

# Package configuration
install(EXPORT {{ namespace.name }}Targets
    FILE {{ namespace.name }}Targets.cmake
    NAMESPACE {{ namespace.name }}::
    DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/{{ namespace.name }}
)

# Create package config file
include(CMakePackageConfigHelpers)

configure_package_config_file(
    "${CMAKE_CURRENT_SOURCE_DIR}/{{ namespace.name }}Config.cmake.in"
    "${CMAKE_CURRENT_BINARY_DIR}/{{ namespace.name }}Config.cmake"
    INSTALL_DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/{{ namespace.name }}
)

write_basic_package_version_file(
    "${CMAKE_CURRENT_BINARY_DIR}/{{ namespace.name }}ConfigVersion.cmake"
    VERSION ${PROJECT_VERSION}
    COMPATIBILITY AnyNewerVersion
)

install(FILES
    "${CMAKE_CURRENT_BINARY_DIR}/{{ namespace.name }}Config.cmake"
    "${CMAKE_CURRENT_BINARY_DIR}/{{ namespace.name }}ConfigVersion.cmake"
    DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/{{ namespace.name }}
)

# Enable testing
enable_testing()
add_test(NAME {{ namespace.name.lower() }}_test COMMAND {{ namespace.name.lower() }}_test)